{
  "name": "karjoo-extension",
  "version": "0.9.5",
  "private": true,
  "description": "Karjoo MV3 multi-board queue executor for Jobinja, JobVision, e-estekhdam, and IranTalent.",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "linkedom": "^0.18.12",
    "typescript": "^5.6.0",
    "vitest": "^3.2.4"
  }
}
