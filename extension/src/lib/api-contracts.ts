/**
 * Small shared constants that mirror the control-plane contract, kept in one
 * place so the api-client and the runner agree without a circular import.
 */

/** Server default match-quality threshold (Foundation DEFAULT_AUTO_APPLY_MIN_SCORE). */
export const DEFAULT_AUTO_APPLY_MIN_SCORE = 0.7;
