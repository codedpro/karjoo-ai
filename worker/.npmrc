# Belt-and-suspenders: never download Playwright browsers during install.
# We depend on `playwright-core` (which downloads NOTHING) and a configurable
# executablePath, so the worker installs+builds+tests in CI without a browser.
# This flag additionally neutralizes any transitive `playwright` install.
playwright_skip_browser_download=1
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
