/**
 * شیم خالی برای `server-only` در محیط تست.
 *
 * بستهٔ واقعی `server-only` وقتی خارج از شرط ماژولی `react-server` (یعنی در
 * Vitest/Node) import شود، عمداً throw می‌کند. در زمان اجرای واقعی Next این درست
 * است و مرز سرور/کلاینت را حفظ می‌کند، اما تست‌های واحدِ ماژول‌های سمت‌سرور را
 * غیرممکن می‌سازد. این فایل صرفاً جای آن بسته را در تست‌ها می‌گیرد و هیچ کاری نمی‌کند؛
 * نگاشت آن فقط در `vitest.config.ts` انجام شده و روی باندل Next اثری ندارد.
 */
export {};
