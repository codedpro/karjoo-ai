/** Server-side source of truth for the published browser extension release. */
export const KARJOO_EXTENSION_VERSION = "0.9.2";

/** Public ZIP served by Next.js from public/. */
export const KARJOO_EXTENSION_DOWNLOAD_PATH = "/karjoo-extension.zip?v=0.9.2";

/** Short release summary shown by the dashboard and extension update UI. */
export const KARJOO_EXTENSION_RELEASE_NOTES =
  "نسخهٔ ۰.۹.۲: اگر رزومهٔ اختصاصیِ یک آگهی ساخته نشود، دیگر کلِ صف متوقف نمی‌شود؛ همان آگهی نیم‌ساعت کنار گذاشته می‌شود و بقیه ادامه می‌یابند. فقط اگر چند بار پشت‌سرهم شکست بخورد — یعنی مشکل عمومی باشد — متوقف می‌شود.";
