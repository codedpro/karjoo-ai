/** Server-side source of truth for the published browser extension release. */
export const KARJOO_EXTENSION_VERSION = "0.9.0";

/** Public ZIP served by Next.js from public/. */
export const KARJOO_EXTENSION_DOWNLOAD_PATH = "/karjoo-extension.zip?v=0.9.0";

/** Short release summary shown by the dashboard and extension update UI. */
export const KARJOO_EXTENSION_RELEASE_NOTES =
  "نسخهٔ ۰.۹.۰: اگر یک سایت چند درخواست پشت‌سرهم را رد کند، ارسال به همان سایت متوقف می‌شود — پیش‌تر ده‌ها درخواستِ ردشده پشت‌سرهم فرستاده می‌شد که خطر محدودشدن حساب شما را داشت. دلیلِ ردشدن هم دیگر «ناموفق» نیست؛ همان پیامی که خودِ سایت داده ثبت و نمایش داده می‌شود.";
