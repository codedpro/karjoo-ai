/** Server-side source of truth for the published browser extension release. */
export const KARJOO_EXTENSION_VERSION = "0.9.6";

/** Public ZIP served by Next.js from public/. */
export const KARJOO_EXTENSION_DOWNLOAD_PATH = "/karjoo-extension.zip?v=0.9.6";

/** Short release summary shown by the dashboard and extension update UI. */
export const KARJOO_EXTENSION_RELEASE_NOTES =
  "نسخهٔ ۰.۹.۶: اگر یک سایت پشت‌سرهم درخواست‌ها را رد کند، فقط همان سایت کنار گذاشته می‌شود و بقیه به کارشان ادامه می‌دهند — پیش‌تر کلِ صف متوقف می‌شد. نام فایل رزومه هم دیگر فارسی نیست و برای هر آگهی یکتاست تا در ذخیرهٔ سایت‌ها گیر نکند.";
