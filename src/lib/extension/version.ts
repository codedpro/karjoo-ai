/** Server-side source of truth for the published browser extension release. */
export const KARJOO_EXTENSION_VERSION = "0.8.5";

/** Public ZIP served by Next.js from public/. */
export const KARJOO_EXTENSION_DOWNLOAD_PATH = "/karjoo-extension.zip?v=0.8.5";

/** Short release summary shown by the dashboard and extension update UI. */
export const KARJOO_EXTENSION_RELEASE_NOTES =
  "نسخهٔ ۰.۸.۵: دکمه‌ها دیگر قفل نمی‌شوند — اگر افزونه پاسخ ندهد، بعد از چند ثانیه پیام می‌دهد به‌جای اینکه همهٔ دکمه‌ها را غیرفعال بگذارد. «توقف» حالا حتی وقتی اجرا در مرورگر مرده باشد کار می‌کند و بی‌درنگ کشف را قطع می‌کند. وضعیت هم راستش را می‌گوید: اگر اجرا متوقف شده باشد، به‌جای «کشف آگهی‌ها» همین را نشان می‌دهد.";
