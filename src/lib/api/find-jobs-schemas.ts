/**
 * اسکیمای اعتبارسنجیِ بدنه‌ی «پیدا کردن شغل‌ها» (Track B — POST /api/apply/find-jobs).
 *
 * عمداً «server-only» نیست: فقط تعریفِ zod (بدونِ راز/I/O) تا هم در route و هم در تستِ
 * واحد بدونِ اصطکاک استفاده شود.
 *
 * این اندپوینت هیچ ورودیِ کاربری‌ای لازم ندارد — کاربر همیشه از نشست گرفته می‌شود و
 * فیلترها از پروفایلِ ذخیره‌شده‌ی همان کاربر خوانده می‌شوند (قاعده‌ی ۴/§۱۰). بدنه عمداً
 * `.strict()` است تا هیچ کلیدِ اضافه (به‌ویژه `userId`ِ جعلی از سمتِ کلاینت) پذیرفته
 * نشود؛ بدنه‌ی خالی/غایب هم مجاز است (به `{}` نگاشت می‌شود).
 */
import { z } from "zod";

/** بدنه‌ی POST /api/apply/find-jobs — بدونِ فیلد؛ کلیدِ ناشناخته → ۴۰۰. */
export const findJobsBodySchema = z.object({}).strict();

export type FindJobsBody = z.infer<typeof findJobsBodySchema>;
